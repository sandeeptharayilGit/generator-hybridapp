language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hybridapp' ]; then cd .. && eval "mv $currentfolder generator-hybridapp" && cd generator-hybridapp; fi

